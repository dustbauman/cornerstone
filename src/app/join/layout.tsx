import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Join the Network — Lodge Signup',
  description: 'Unlock Tyrian for your lodge. List lodge-verified professionals, post service requests, and give members access to the Masonic business network.',
}

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return children
}
